1. What is Azure ARM?
    - Azure Resource Manager
    - It's a control plane for azure which is the service that creates, updates. deletes or manages all the azure resources.
    - Every azure request go through ARM from portal,CLI and terraform all talks to ARM
    - ARM templates(json/bicep) used as Iac.
